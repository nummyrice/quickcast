import React from 'react';
import './Widgets.css';

const Widgets = () => {
    return(
        <div id='widgets'>
            <div className={`example_widgets`}>{'Widgets to be added...'}</div>
        </div>
    )
}

export default Widgets;
